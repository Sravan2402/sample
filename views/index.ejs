<!DOCTYPE html>
<head>

</head>
<body>
    <table>
        <% users.forEach( user=>{ %>
        <tr>
            <td><%= user.name %></td>
            <td><%= user.age %></td>
        </tr>
        <%}) %>
    </table>
</body>
</html>